.ua/
.mcp.json
.syncthing*
.agents/skills/

# The root package.json is a dependency-free task runner (see its description field).
# Anything npm leaves behind up here is incidental and shouldn't be committed --
# server/package-lock.json is the one that matters.
/node_modules/
/package-lock.json

# Project-specific session handoff notes -- not part of the repo's own history/docs
/E40-HANDOFF.md
# Agent scratch files -- not part of the repo's own history/docs
/scratchpad/
# Added by code-review-graph
.code-review-graph/
.envrc
